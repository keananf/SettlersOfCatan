# Settlers of Catan
Settlers of Catan Implementation in Java using LibGDX

## Building
### Linux and macOS
`./gradlew build`

### Windows
#### (Powershell)
`./gradlew.bat build`
#### (Command shell)
`gradlew build`
